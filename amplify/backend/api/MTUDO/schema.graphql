type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  username: String
  email: String
  avatarkey: String
  description: String
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  isComplete: Boolean
}
 